# AI-hackson-toio

全日本AIハッカソン向けのデモ。テーマは **「友」**。
toio Core Cube 2台で鬼ごっこをしつつ、**片方が動けなくなったら助けに行き、助けたら仕切り直す**挙動をアルゴリズムで実装している。

## デモの流れ

| フェーズ | 挙動 |
| --- | --- |
| `CHASE` | 鬼は逃げ手の予測位置を追い、逃げ手は「鬼からの反発＋壁からの反発」の合成方向へ逃げる |
| `RESCUE` | 片方が座標をロスト（マット外）または障害物に嵌ったら、もう片方が救助に向かう |
| `REGROUP` | 救助・捕獲のあとに定位置へ戻って仕切り直す |
| `HELP_NEEDED` | 救助に失敗したら黄点滅で人の手を待つ（無限リトライで暴れさせない） |

役割交代は**捕獲したときだけ**。救助を挟んでも鬼は交代しない（救助をペナルティにしない）。

## 実行方法

Web Bluetooth は **HTTPS または localhost でしか動かない**。`file://` で直接開いても toio に接続できない。

### p5.js Web Editor

1. 新規スケッチを作成する
2. `index.html` の内容を貼り付ける（p5.toio の `<script>` を読み込むため）
3. `tag-game.js` をファイルとして追加し、内容を貼り付ける

### ローカル

```bash
python3 -m http.server 8000
# ブラウザで http://localhost:8000/ を開く
```

## 操作

| キー | 動作 |
| --- | --- |
| `c` / 画面のボタン | toio を接続する（2台つなぐとゲームが始まる） |
| `r` | 強制的に仕切り直す（`HELP_NEEDED` からの手動復帰も兼ねる） |
| `space` | 一時停止 / 再開 |
| `f` | 全画面表示 |

パラメータ（速度・捕獲距離・検知しきい値など）は `tag-game.js` 冒頭の定数にまとめてあり、実機で調整する前提。

## ファイル

| ファイル | 内容 |
| --- | --- |
| `index.html` | CDN 読み込みとエントリ |
| `tag-game.js` | ゲーム本体（状態機械・追跡/逃走・ロスト/スタック検知・救助） |
| `sample.js` | p5.toio の 2D 表示サンプル（参考用。`index.html` からは読み込まない） |
| `docs/report/` | toio の実装可能な挙動・API リファレンスの調査レポート |

## クレジット

- [p5.toio](https://tetunori.github.io/p5.toio/)（MIT License）— 非公式ライブラリ
- toio™ 技術仕様書（CC BY 4.0） https://toio.github.io/toio-spec/
